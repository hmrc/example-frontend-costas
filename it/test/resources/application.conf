mongodb.uri = "mongodb://localhost:27017/example-frontend-costas-integration"

pekko {
  log-dead-letters = 10
  log-dead-letters-during-shutdown = on
}

mongo-async-driver {
  pekko {

    loglevel = ERROR
  }
}
